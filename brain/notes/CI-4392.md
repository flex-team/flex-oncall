# CI-4392: 평가 생성 중, 알 수 없는 오류가 계속 뜹니다.

## 증상

- 회사: athometrip (Customer ID: 202640)
- 문의자: jiwonwoo@athometrip.com (티켓 기재 jwwoo93@gmail.com은 오류)
- 라벨: 성과관리
- 평가 ID: 01knrh8saw6rgdm6v2zz74fera
- 평가 생성 화면에서 "알 수 없는 오류"가 계속 발생
- 새로고침, 재진입해도 동일 증상 지속

## 문제 평가

> Render 타입 · 단일 고객 · 낮음

- **성격**: 평가 생성 4번째 단계(EVALUATION_QUESTIONNAIRE)에서 "알 수 없는 오류" 표시. 백엔드 에러 아닌 프론트엔드 렌더링 이슈로 추정.
- **범위**: customer_id=202640 단일 고객 — 근거: 다른 고객 문의 없음
  - 진행형: 불명 (재현 여부 미확인)
- **긴급도**: 낮음 — 평가 생성 중단이지만 데이터 유실/금전 영향 없음
- **조사 방향**: 프론트엔드 에스컬레이션 / COOKBOOK: 해당 없음

## 원인 분석

### 서버 측 조사 결과 (에러 없음)

1. **access log** (최근 24시간, prod)
   - 평가 ID `01knrh8saw6rgdm6v2zz74fera` 관련 254건 요청 전부 HTTP 200
   - 4xx/5xx 에러: 0건
   - 주요 호출 패턴: GET /action/v3/evaluation/draft/.../evaluation-steps, GET /api/v3/evaluation/.../forms/.../draft

2. **application/consumer 에러 로그**: 0건 (review-api 서비스, customer 202640)

3. **draft_evaluation DB 상태** (정상)
   - mode=CREATE, evaluation_type=NORMAL, title=[수습평가] 소은지 님
   - deleted_at=NULL
   - completed_steps_json: ["BASIC_INFORMATION", "EVALUATION_FACTORS_AND_STEPS", "EVALUATION_ITEMS"]
   - 사용자가 진행 중인 단계: EVALUATION_QUESTIONNAIRE (4번째, URL의 eval-est 파라미터)

4. **Feature activation**: evaluation_feature_activation, customer_flex_subscription_feature 모두 0건이나 API가 200 응답이므로 feature control은 통과하고 있음

### FE 원인 확정 (이해나 님 분석)

구리뷰 템플릿 불러오기(`ImportReviewTemplateModal`)로 생성된 점수형 질문의 옵션 description이 FE 글자수 제한(100자)을 초과하여 발생.

- **경로**: 구리뷰 템플릿 불러오기 → `POST /blocks/bulk` API로 평가지 블록 일괄 생성 → 구리뷰에는 옵션 description 글자수 제한 없음 → 100자 초과 텍스트가 그대로 저장
- **에러 발생 시점**: "단계별 평가지" 화면 진입 시, FE Zod 스키마(`ScoreQuestionSchema`)에서 `descriptions: z.array(z.string().max(100))`으로 검증 → 100자 초과 항목에서 실패 → "알 수 없는 오류" toast
- **해당 데이터 예시**: `[매우 우수] 주도적으로 문제를 해결하고, 팀에 긍정적 영향을 줌...` (약 105자)
- form ID: `01knrrr0cf13ve91bbsj18am4h`

### 데이터 분석 (BE)

`form_question.score_setting` JSON 내 `metadata.elements[].description` 기준:

| 항목 | 값 |
|------|-----|
| 전체 옵션 description 수 | 46,533건 |
| 최대 길이 | **129자** |
| 평균 길이 | 약 13자 |
| 100자 초과 건수 | **98건** |

100자 초과 데이터는 모두 동일한 구리뷰 템플릿에서 유래. 최대 129자.

### 판단

**verdict**: FE validation과 구리뷰 데이터 간 글자수 제한 불일치. 백엔드 버그 아님. FE 정책 결정 필요.

## 영향 분석

- 구리뷰 템플릿 불러오기로 생성된 점수형 질문 중 옵션 description이 100자를 초과하는 경우에만 발생
- 전체 46,533건 중 98건 (0.2%) 해당
- 동일 구리뷰 템플릿을 사용하는 다른 고객에서도 동일 문제 발생 가능

## 해결안

FE 팀(이해나, 이윤주)에서 검토 중:
1. **유저 자가 해결**: 각 옵션 description을 100자 이내로 줄임
2. **정책 변경**: 옵션 설명 maxLength 제한을 늘림 (130자면 기존 데이터 수용 가능)
3. **FE 불러오기 시 truncate**: 구리뷰 템플릿 불러오기 시 평가의 글자수 제한에 맞게 자름

## 영향 범위

- customer_id=202640 포함, 동일 구리뷰 템플릿 사용 고객
- 전체 form_question 기준 98건 / 46,533건 (0.2%)

## 해결

- FE PR: [fix(evaluation): 점수형 옵션 설명 maxLength 100자 → 200자로 확대](https://github.com/flex-team/flex-frontend-apps-performance-management/pull/4838)
- 담당: 이해나 (FE)
- 유저 임시 해결: 옵션 description을 100자 이내로 줄이면 즉시 해소 (CS 안내)

## 비고

- Linear: CI-4392
- draft_evaluation_step, form_draft, draft_evaluation_step_form_mapping 테이블은 DB 화이트리스트 미등록으로 직접 조회 불가
- Sentry에도 에러 미포착 (FE Zod validation 실패는 Sentry에 보고되지 않는 구조)

## 각주

- access log 검색: be-access 인덱스, 2026-04-09~10, evaluation ID 기준
- application log 검색: be-api 인덱스, review-api 서비스, ERROR/WARN 레벨
- DB 조회: flex_review.draft_evaluation, flex_review.evaluation_feature_activation, flex.customer_flex_subscription_feature, flex_review.form_question
- FE 원인 분석: 이해나 님 (Slack 스레드 2026-04-10 11:55)
